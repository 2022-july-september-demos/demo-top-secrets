module.exports = class User {
  static async function insert({email, passwordHash}){
    // insert new row in the database
    // return a User object for the new row
  }
};

module.exports = { User };
